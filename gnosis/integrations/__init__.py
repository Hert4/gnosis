"""Integrations — adapters for third-party frameworks.

Each module is optional and only imported on demand. Core framework has
zero hard dependency on langchain / langgraph. Install with:
    pip install retrival-framework[langchain]
    pip install retrival-framework[langgraph]
"""
